## Field hotspot notes

Use `scripts/setup_offline_hotspot.sh` once. The profile is persistent and autoconnects after reboot. The Raspberry Pi uses `192.168.50.1` on wlan0 and NetworkManager supplies local DHCP/shared networking. The application remains on port 8080 and the mobile page is `/mobile`.

No Internet connection is needed after setup.
