# Offline hotspot ready

Release 0.8.20 field networking is ready for Raspberry Pi validation.

The hotspot uses NetworkManager shared mode on wlan0 with 192.168.50.1/24. The wired Ethernet profile is not changed. GateKeeper continues to serve the mobile dashboard on port 8080.
