# 0.8.20 offline hotspot

This field-mode release adds a Raspberry Pi local Wi-Fi access point using NetworkManager. It is intended for operation with no Internet access. The Android phone connects directly to the Pi and uses the existing mobile dashboard at `http://192.168.50.1:8080/mobile`.

The existing OCR, Plate Lock and GPIO logic are unchanged.
