# Field scripts

- `setup_offline_hotspot.sh` — configure the persistent local Wi-Fi access point.
- `enable_offline_hotspot.sh` — enable the configured hotspot.
- `disable_offline_hotspot.sh` — disable the hotspot and leave Ethernet untouched.
